initSidebarItems({"fn":[["clamped_laplace_mechanism","Implements clamped Laplace mechanism via the base-2 exponential mechanism Outputs an `f64` in the range `[lower_bound, upper_bound]` of the form `lower_bound + gamma*i`. ## Arguments   * `eta`: the base-2 privacy parameter   * `lower_bound`: the lower bound of the range for the Laplace mechanism   * `upper_bound`: the upper bound of the range for the Laplace mechanism   * `target`: the target value (value to approximate)   * `gamma`: the granularity of the outcome space   * `rng`: a random number generator"]]});